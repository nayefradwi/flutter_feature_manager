name: flutter_feature_manager
description: "A new Flutter package project."
version: 0.0.1
# homepage:

environment:
  sdk: '>=3.4.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  firebase_remote_config: ^4.4.5
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:
